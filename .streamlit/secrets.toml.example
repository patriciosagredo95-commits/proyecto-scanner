# Copie este archivo a .streamlit/secrets.toml (NUNCA lo suba a git) y complete
# con la cadena de conexión "pooled" de su proyecto Neon.
[connections.postgresql]
url = "postgresql+psycopg2://<usuario>:<password>@<host>-pooler.<region>.aws.neon.tech/<db>?sslmode=require"
